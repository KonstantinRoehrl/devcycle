node_modules/
.release-notes.md
scripts/redaction-terms.local.txt
.devcycle/
.superpowers/
.DS_Store
.worktrees/
graphify-out/

# Everything under docs/ is ignored and each tracked surface is re-included by name, so a new
# scaffolding directory is ignored by default and a new tracked doc is a deliberate line — the
# inverse of naming each stale directory individually and missing the next one.
# Audit reports stay trackable as markdown — re-including docs/audits/ and then ignoring everything
# inside it made every report invisible to git status, so a run's own record could never be
# committed. docs/audits/* keeps stray non-markdown artifacts out; !docs/audits/*.md carries the
# reports themselves. scripts/validate.mjs check 25 enforces that pairing.
docs/*
!docs/*.md
!docs/comparisons/
!docs/devcycle/
!docs/audits/
!docs/case-studies/
!docs/design/
!docs/decisions/
!docs/configuration/
!docs/pipeline/
!docs/playbooks/
docs/audits/*
!docs/audits/*.md
# Routing advisories are one-run records of this repo's own dispatch costs: written locally, never
# shipped to installers. Other repos do commit their copy — that is the `commit` cell in
# references/config.md § Doc tracking, which already scopes every such cell to what the host repo's
# own ignore rules allow, and this repo's answer is no.
docs/devcycle/routing-advisories.md
